<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
  <!-- Google Play restricts these: USE_EXACT_ALARM is for alarm-clock and calendar apps, and
       REQUEST_IGNORE_BATTERY_OPTIMIZATIONS needs a policy exemption. The Play build relies on
       SCHEDULE_EXACT_ALARM, which the user grants from Ayarlar > Alarm Güvenilirliği. -->
  <uses-permission android:name="android.permission.USE_EXACT_ALARM" tools:node="remove"/>
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" tools:node="remove"/>
</manifest>
